# Система управления спутниковой группировкой

Проект демонстрирует принципы ООП на примере симуляции управления спутниками.  
Реализована работа со спутниками двух типов: **CommunicationSatellite** и **ImagingSatellite**, а также с их группировкой (**SatelliteConstellation**).  

---

## 📌 Задача

Создать систему управления спутниками, где можно:

- Создавать спутники разных типов;
- Управлять их состоянием (включение/выключение, расход энергии);
- Выполнять миссии (передача данных, съемка территории);
- Формировать и управлять группировками спутников.

---

## 🏗 Архитектура проекта

Архитектура построена по принципам ООП:  

- **Класс `Satellite`** — абстрактный базовый класс с атрибутами `name`, `isActive`, `batteryLevel` и методами управления питанием и активацией.  
- **Класс `ImagingSatellite`** — наследник `Satellite`, реализует съемку территории (`takePhoto`) и учет сделанных снимков.  
- **Класс `CommunicationSatellite`** — наследник `Satellite`, реализует передачу данных через полосу пропускания (`bandWidth`).  
- **Класс `SatelliteConstellation`** — агрегирует спутники, позволяет добавлять их в группировку и выполнять миссии для всех спутников.

**Диаграмма классов**:

<img width="592" height="567" alt="Снимок экрана 2025-12-07 в 20 36 46" src="https://github.com/user-attachments/assets/fa0caabf-ea80-4515-8944-b85dd3cb6d66" />
